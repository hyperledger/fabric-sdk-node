name: Build

on:
  # Merge build (including publishing) runs on Azure Pipelines
  # push:
  #   branches:
  #     - release-2.2
  pull_request:
    branches:
      - release-2.2

jobs:
  build:
    uses: ./.github/workflows/test.yml
