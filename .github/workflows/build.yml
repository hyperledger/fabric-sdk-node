name: Build

on:
  # Merge build (including publishing) runs on Azure Pipelines
  # push:
  #   branches:
  #     - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/test.yml
